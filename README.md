# Career Quest · KOMO

AI-навигатор развития сотрудника для HackAlem AI / Halyk Bank. Сотрудник выбирает цель, получает объяснимые рекомендации, начинает или планирует активность и видит изменение навыков после подтверждения выполнения. HR видит дефициты компетенций, участие и сотрудников без актуального шага, импортирует новые профили и историю.

**Стек:** Python 3.12 / FastAPI / SQLAlchemy / PostgreSQL / Redis; Next.js / TypeScript. Модульный монолит, четыре сервиса Docker Compose. Данные синтетические.

## Быстрый запуск

Нужны Docker Desktop / Engine и Compose 2.24+; свободные порты 3000 и 8000. Проверено с Docker 28.5.1 и Compose 2.40.3. Первая сборка скачивает образы и зависимости. Node и Python на хосте для запуска приложения не нужны.

1. Откройте терминал в корне репозитория.
2. Для AI создайте локальный `.env` по `.env.example` и укажите действующий API-ключ. Существующий `.env` не перезаписывайте. Без ключа доступен явно обозначенный резервный подбор.
3. Запустите:

```sh
docker compose up --build
```

Откройте **http://localhost:3000**. Миграции, исходный набор и демо-аккаунты создаются автоматически. Для фонового запуска добавьте `-d`.

| Роль | Логин | Пароль по умолчанию |
| --- | --- | --- |
| Сотрудник E0028 | `employee` | `employee-demo-2026` |
| HR | `hr` | `hr-demo-2026` |

Это публичные демо-доступы к синтетическому локальному приложению. Сотрудник видит только свой профиль; HR читает профили и импортирует данные, но не меняет чужие цели и участия.

`docker compose down` сохраняет данные. **Не используйте `down -v` для рабочего проекта.** Повторный запуск не сбрасывает цели, импорт или завершения. PostgreSQL и Redis не публикуют порты, web доступен только на localhost.

## Конфигурация и реальный AI

| Переменная | Назначение |
| --- | --- |
| `OPENAI_API_KEY` | Основной API-ключ OpenAI Platform |
| `OPENAI_KEY` | Поддерживаемое альтернативное имя; стандартное имеет приоритет |
| `OPENAI_MODEL` | По умолчанию `gpt-4.1-mini-2025-04-14` |
| `EMPLOYEE_ID` | Привязка демо-сотрудника; по умолчанию E0028 |
| `EMPLOYEE_PASSWORD`, `HR_PASSWORD` | Пароли демо-аккаунтов; применяются при запуске |
| `SESSION_SECRET` | Подпись cookie; без переменной генерируется один раз в БД |
| `APP_EXTRA_ORIGINS` | Дополнительные разрешенные origin через запятую; по умолчанию localhost:3000 и 127.0.0.1:3000, чтобы локальный вход работал вместе с настроенным адресом |
| `API_PORT` | Локальный порт API для диагностики, по умолчанию 8000 |
| `WEB_PORT`, `APP_ORIGIN` | По умолчанию `3000` и `http://localhost:3000`; должны соответствовать друг другу |

После смены ключа:

```sh
docker compose up -d --force-recreate api
docker compose exec -T api python -m app.smoke_ai --fresh
```

`--fresh` проверяет новый запрос без Redis-кеша. Успех: `source=ai`, `cached=false`, непустые шаги; скрипт завершится ошибкой при fallback. Подписка ChatGPT не заменяет API-доступ. Таймаут модели — 8 секунд, автоматических повторов нет. При отказе, таймауте или неверном ответе показывается **«Резервный подбор»**, который не считается проверкой AI.

В модель передаются синтетические роль, грейд, стаж, цель, навыки, допущенные активности и агрегаты истории. Имя, ID сотрудника и сырая история не передаются; `store=False`. Ключ находится только в backend, не выводится в интерфейс и не включается в Git или Docker-образ.

Для независимой проверки экспертам нужен отдельный действующий демонстрационный API-доступ, переданный через разрешенный организатором канал. Наличие нашего локального ключа не подтверждает передачу доступа. Без этого условия готовность к независимой AI-проверке не заявляется.

## Сквозной сценарий сотрудника

1. Войти как сотрудник: открыть цель, соответствие требованиям, навыки и историю.
2. Нажать **«Изменить цель»**, выбрать роль и грейд, проверить требования и **«Сохранить цель»**. Можно вернуть автоматическую цель. Текущий грейд и навыки не меняются; меняется цель сравнения.
3. Нажать **«Подобрать с AI»** или **«Обновить подбор»**. Открыть **«Почему этот шаг»**: минимум три группы проверяемых фактов, ожидаемые навыки и прогресс.
4. Во вкладке **«Подходящие активности»** выбрать самостоятельный курс → **«Начать»** → **«Начать активность»**. Он появится в **«Мои активности»** со статусом «В процессе», без начисления навыков.
5. После прохождения вне платформы открыть **«Продолжить»** → **«Отметить выполненным»**. Показаны изменения до/после, обновлены история и подбор. В демо это собственное подтверждение сотрудника, без проверки учебной системой.
6. Обновить страницу: результат сохранен. Изменение цели не скрывает начатые активности и не запрещает их завершение только из-за новой цели.
7. Для мероприятия по расписанию: **«Запланировать»** → дата из каталога → **«Запланировать участие»**. Это локальная запись, не внешняя регистрация. До даты сессии завершение запрещено. Локальное незавершенное участие можно отменить.

На уже использованном E0028 курс Cloud Certification Prep может быть завершен. Это корректно: повторное начисление запрещено. Для повторения полного демо используйте новый профиль, описанный ниже, без очистки рабочей БД.

## HR и импорт


На `/hr/import` выберите формат и нажмите **«Скачать шаблон»**: Excel (.xlsx) или JSON с синтетическим примером. Замените пример своими данными и загрузите в **«Профили сотрудников»**. Один файл может включать и профили, и историю — тогда второй файл не нужен.

- **Excel:** лист `employees` — полные профили, `employee_skills` — строки `employee_id, skill_id, level`, `history` — история. Листы `roles`, `skills`, `events` содержат справочники; `instructions` — инструкцию. Названия листов и колонок сохраняйте, порядок колонок можно менять. Пустые строки пропускаются. Даты — ячейки Excel или `YYYY-MM-DD`. Формулы перед загрузкой замените значениями. Старые `.xls` пересохраните как `.xlsx`.
- **JSON:** исходный `{meta, employees}`, массив профилей или общий `{meta, employees, history}`. При отсутствии `meta` применяется Career Quest 1.0, срез 2026-10-01. Для отдельной истории — массив записей или `{history: [...]}`.
- **История отдельно:** Excel с листом `history`, JSON или CSV в UTF-8. CSV принимает запятую или точку с запятой, порядок исходных колонок свободный. Можно совместить JSON профилей и Excel/JSON/CSV истории. Один вид данных нельзя передавать сразу в двух файлах.
- Для импорта только профилей удалите строки примера на `history` или сам лист. Для импорта только истории оставьте `history` и справочники. Пустой `employee_skills` означает отсутствие оценок. Профили заменяются целиком — это не частичное обновление отдельных полей.
- Лимиты: 4 МБ на файл, 2000 профилей / 20 000 участий за запрос; распакованный Excel до 32 МБ. Ошибки структуры Excel указывают лист, строку и поле, ошибки связей — ID профиля или участия. Новые ID поддерживаются.

API шаблона: `GET /api/v1/hr/import/template?format=xlsx|json`, только для HR. Шаблоны хранятся в `backend/app/features/data_import/templates` и входят в Docker-образ. OpenAPI и TypeScript-типы обновлены.

Проверяются значения, статусы, ссылки, роли/грейды, навыки и дата среза. Повторные ID внутри пакета отклоняются; идентичный повторный импорт не добавляет записи. Обновления атомарны. Подтвержденные в приложении выполнения нельзя молча перезаписать импортом; несовместимый срез отклоняется.

Три собственных синтетических примера в `docs/fixtures/` проверяют критический разрыв против минимального навыка, prerequisites/потолки и смену роли без истории. Это **не профили жюри**.


## Повторяемый E2E и подготовка нового демо

По выбранному режиму E2E **добавляет данные в рабочую демо-БД**. Каждый запуск использует новые `QA_R2_…` ID; исходный E0028 и его завершения не сбрасываются. Подготовка учетной записи выполняется локальным CLI, публичной регистрации нет.

Чтобы вручную повторить демо:

```sh
mkdir -p artifacts/round2
docker compose exec -T api python -m app.qa_demo prepare > artifacts/round2/demo.json
python3 - <<'PY'
import json
from pathlib import Path
root = Path('artifacts/round2')
data = json.loads((root / 'demo.json').read_text())
(root / 'employees.json').write_text(json.dumps(data['bundle'], ensure_ascii=False))
(root / 'history.csv').write_text(data['history_csv'])
print('ID для входа:', data['employee_id'])
PY
```

Под HR импортируйте созданные `employees.json` и `history.csv`. Затем подставьте выведенный ID:

```sh
docker compose exec -T api python -m app.qa_demo account --employee-id QA_R2_ВАШ_ID_FLOW
```

CLI создаст аккаунт только для уже импортированного профиля `QA_R2_`. Логин равен ID, демонстрационный пароль — `qa-demo-r2`. Пройдите сценарий сотрудника с курсом Cloud Certification Prep, затем проверьте его профиль под HR.

### Автоматические проверки

Backend-тесты тоже добавляют синтетические QA-профили. Провайдер в них подменен тестовой фикстурой, PostgreSQL настоящий:

```sh
docker compose exec -T api python -m pytest -q
```

Отдельная проверка реальной модели на четырех новых профилях — обычный, критический разрыв с пропусками, требования к участию/потолки, смена роли без истории:

```sh
docker compose exec -T api python -m app.qa_demo verify-ai
```

Она импортирует новые данные и проверяет качество выбора, факторы и ответ без кеша до 10 секунд. Ошибка качества дает ненулевой код завершения; HTTP 200 недостаточно.

Браузерные тесты запускаются с хоста, нужны Node.js 22 и браузер:

```sh
cd frontend
npm ci
npm run typecheck
npm run build
npx playwright install chromium
npm run test:e2e
```

Если установлен Google Chrome, вместо скачивания Chromium: `PLAYWRIGHT_CHANNEL=chrome npm run test:e2e`. Используется отдельный временный браузерный профиль. По умолчанию приложение — `http://localhost:3000`; Docker Compose запускается из корня репозитория. При измененных демо-паролях задайте `HR_PASSWORD` и `EMPLOYEE_PASSWORD` в окружении запуска тестов.

E2E проходит импорт, вход нового сотрудника, цель, **реальный AI**, начало/завершение, reload, планирование/отмену и HR. При отсутствии рабочего API-ключа тест основного сценария завершится ошибкой. Скриншоты и результат: `artifacts/round2/`; ID последнего профиля — `last-run.json`. Эти артефакты игнорируются Git.

### Проверка чистого запуска

Используйте новый project с собственным volume, не затрагивая рабочие данные:

```sh
API_PORT=8002 WEB_PORT=3002 APP_ORIGIN=http://localhost:3002 docker compose -p career-quest-clean-r2 up -d --build
```

Проверьте http://localhost:3002, вход и `/api/v1/ready`. Остановка без удаления данных:

```sh
docker compose -p career-quest-clean-r2 down
```

## Архитектура и правила

`Browser → Next.js /api/v1 proxy → FastAPI → PostgreSQL / Redis / OpenAI Responses API`.

Фичи backend: `identity`, `catalog`, `employees`, `development`, `recommendations`, `hr_analytics`, `data_import`. Все предметные расчеты принадлежат `development`; frontend показывает данные API. PostgreSQL — источник истины, Redis — необязательный кеш на 5 минут.

- Исходный набор: 200 сотрудников, 40 событий, 60 навыков, 32 профиля ролей/грейдов, 2743 участия. Исходные файлы не изменяются.
- Расчетное сегодня фиксировано: **2026-10-01**. Оно не двигается с системным временем; часы сервера нужны для TTL и служебных отметок.
- Базовые навыки — на `last_review_date`. Последующие завершения учитываются по формуле `max(current, min(5, max_level, current + gain))`.
- Для импортированной self-paced истории дата строки — приближение даты учета. Завершение в день оценки считается включенным в базу; новое локальное подтверждение в тот же день начисляется после базы.
- Начало, планирование и отмена не дают навыков. Повторное завершение защищено транзакцией, блокировкой и квитанцией идемпотентности; повтор с другим содержимым дает конфликт.
- Обязательные события не рекомендуются. Исторический прирост EV_004 учитывается. Завершенные события не повторяются, кроме отдельных сессий EV_036.
- Допуски: текущая роль/грейд, требования к участию, расписание, история и полезный прирост. При отсутствии прямого шага проверяется один подготовительный переход.
- Автоматическая цель — следующий грейд своей роли; для Lead — поддержание текущих требований. Ручная цель допускает другую роль и любой существующий грейд. Повышения грейда автоматически нет.
- Прогресс = `sum(min(current, required)) / sum(required) × 100`; пустые требования дают 100%. Прогноз каждой активности независим от других карточек.
- Backend создает факты; модель выбирает 1–3 активности и факты минимум из трех групп. Проверяются ID, уникальность и принадлежность фактов. Числа и объяснения берутся из проверенных данных.
- Актуальность определяется `context_version`: хеш данных сотрудника, его цели и истории, каталога, расчетной даты и версии логики. Действие другого сотрудника не сбрасывает подбор. Общая ревизия остается диагностической; HR не запускает массовые AI-запросы.
- HR-завершенность = completed / все статусы, кроме planned и cancelled. В таблице показаны все группы, включая overdue; сумма групп равна числу участий.

API: http://localhost:3000/api/v1/docs. Swagger загружает UI с CDN; JSON `/api/v1/openapi.json` доступен локально. Снимок — `docs/openapi.json`; типы — `frontend/src/shared/api/schema.d.ts`.

Дополнения второго прохода: `GET /catalog/role-profiles`, `PATCH /employees/{id}/career-goal`, `POST /employees/{id}/activities/{event_id}/start`, `POST /employees/{id}/participations/{record_id}/cancel`, `refresh=true` у POST рекомендаций. Все под `/api/v1`. Ключ идемпотентности обязателен для действий участия. После изменения контракта обновите OpenAPI и выполните `npm run api:types` в frontend.

## Соответствие ТЗ и оценке

Источник: [официальное ТЗ Career Quest](https://docs.google.com/document/d/18SlxWxz_Vj_eag_UfEQ6t6g59jCxGTgu5x4QoylDbqI/edit?tab=t.0), прочитано 23.09.2026. Веса групп: работоспособность 25, техническая реализация 25, README/воспроизводимость 25, ценность 15, потенциал/оригинальность 10. Они не гарантируют баллы за наличие отдельных функций.

| Требование | Реализация | Проверка | Критерии |
| --- | --- | --- | --- |
| Профиль и траектория | Кабинет, навыки, цель, история, подходящие шаги | E2E сотрудника и HR, смена роли/автоцель | Работоспособность, ценность |
| 1–3 AI-шага | Responses API, проверенные кандидаты, проверка ответа | `verify-ai`, свежий вызов, новые профили | Работоспособность, техническая реализация |
| Минимум 3 фактора | Факты о профиле, навыках, истории и цели | Проверка fact IDs, принадлежности и групп | Техническая реализация, ценность |
| Прогресс после выполнения | Общий расчет, транзакция, идемпотентность | Start/complete, конкуренция, reload/restart | Работоспособность, техническая реализация |
| HR-view | Дефициты, отсутствие шага, участие по статусам | Сверка агрегатов и просмотр после E2E | Работоспособность, ценность |
| Проверочные профили | Атомарный импорт JSON/CSV с новыми ID | Импорт в браузере, повтор, ошибочный пакет | Работоспособность, воспроизводимость |
| Приватность и права | Собственный профиль, отдельная роль HR | 401/403, запрет чужих изменений | Техническая реализация |
| Скорость | AI-таймаут 8 с, кеш, локальные расчеты | Обычные ответы <2 с, AI <10 с | Работоспособность, техническая реализация |
| Запуск и документация | Compose, миграции, seed, инструкция | Чистая БД и новый project | README/воспроизводимость |

Смена цели и локальное участие — выбранные расширения, а не отдельные обязательные пункты ТЗ. Их ценность — сделать следующий шаг исполнимым в рамках демонстрационного учета. Материалы курсов, внешняя регистрация и проверка реального обучения не реализованы. Нет рейтингов, наград за обязательные процессы, реальных персональных данных, SSO, чата или автоматического повышения грейда.

Результаты: [docs/VERIFICATION.md](docs/VERIFICATION.md). Статус: [TASK.md](TASK.md). Компоненты, лицензии и источники: [docs/SOURCES.md](docs/SOURCES.md). Репозиторий организатора: `BAITC-Hacks/hack-d98d69cd-komo`.

Коммиты и push выполняются только по отдельной команде пользователя. `.env` вне индекса; это не очищает прошлую историю. Наличие проекта на localhost не означает официальной сдачи
